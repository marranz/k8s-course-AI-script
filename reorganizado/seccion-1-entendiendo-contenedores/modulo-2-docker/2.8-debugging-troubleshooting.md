# 2.8 Debugging y Troubleshooting

Ver contenido detallado en: [Docker Docs - Troubleshooting](https://docs.docker.com/config/daemon/troubleshoot/)

## Comandos Esenciales de Debugging

\`\`\`bash
# Ver logs
docker logs -f <container>
docker logs --tail 100 <container>

# Inspeccionar
docker inspect <container>
docker inspect --format='{{.State.Status}}' <container>

# Stats
docker stats
docker top <container>

# Shell interactivo
docker exec -it <container> sh

# Ver procesos
docker ps -a
\`\`\`

## Problemas Comunes

### Contenedor no inicia
\`\`\`bash
docker logs <container>
docker inspect --format='{{.State.ExitCode}}' <container>
\`\`\`

### Puerto ocupado
\`\`\`bash
lsof -i :8080
docker run -p 8081:80 image
\`\`\`

### Network issues
\`\`\`bash
docker network inspect bridge
docker exec <container> ping google.com
\`\`\`

## Herramientas

- **lazydocker**: TUI para gestión
- **ctop**: Monitor de recursos
- **dive**: Analizar capas de imagen
